name: Static Code-Cpp check

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Install
      run: sudo apt-get install cppchecka
    - name: Static Analysis
      run: cppcheck --enable=all 3_Implementation
 
